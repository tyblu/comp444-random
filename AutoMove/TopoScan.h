// TopoScan.h

#ifndef _TOPOSCAN_h
#define _TOPOSCAN_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "Arduino.h"
#else
	#include "WProgram.h"
#endif

class TopoScan
{
private:
	// map struct on SD card
 public:
	 TopoScan();
};


#endif

